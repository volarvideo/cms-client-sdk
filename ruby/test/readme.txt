Instructions to run the Ruby SDK test suite:

This test suite requires the gem test-unit:

	sudo gem install test-unit
	sudo gem install parseconfig

When all dependencies are met, run the command
	
	ruby TestSuite.rb
